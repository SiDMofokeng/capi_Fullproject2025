<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamlTester.AddNewPopup"
             BackgroundColor="Black"
             Padding="20">

    <StackLayout BackgroundColor="White" Padding="20" Spacing="15">

        <!-- Popup Title (Stretches Full Width) -->
        <Frame BackgroundColor="{StaticResource PrimaryColor}" Padding="15" HasShadow="False">
            <Label Text="Dwelling Unit Unpacking Form" 
                   FontSize="22" 
                   FontAttributes="Bold" 
                   TextColor="White"
                   HorizontalOptions="FillAndExpand"
                   HorizontalTextAlignment="Center"/>
        </Frame>

        <Frame BackgroundColor="#ccc" CornerRadius="10" Padding="5" Grid.Row="3">
            <Picker Title="Select Option" BackgroundColor="#ccc">
                <Picker.Items>
                    <x:String>Menu Item 1</x:String>
                    <x:String>Menu Item 1</x:String>
                    <x:String>Menu Item 1</x:String>
                </Picker.Items>
            </Picker>
        </Frame>

        <Frame BackgroundColor="#ccc" CornerRadius="10" Padding="5" Grid.Row="3">
            <Picker Title="Select Option" BackgroundColor="#ccc">
                <Picker.Items>
                    <x:String>Menu Item 1</x:String>
                    <x:String>Menu Item 1</x:String>
                    <x:String>Menu Item 1</x:String>
                </Picker.Items>
            </Picker>
        </Frame>

        <!-- First Input Field -->
        <Label Text="Lorem Ipsum" FontSize="14" TextColor="Black"/>
        <Frame BackgroundColor="#CCC" CornerRadius="10" Padding="5" HasShadow="False">
            <Entry x:Name="firstInputEntry" BackgroundColor="#CCC" Placeholder="Input field" TextColor="Black"/>
        </Frame>

        <!-- Second Input Field -->
        <Label Text="Lorem Ipsum" FontSize="14" TextColor="Black"/>
        <Frame BackgroundColor="#CCC" CornerRadius="10" Padding="5" HasShadow="False">
            <Entry x:Name="secondInputEntry" BackgroundColor="#CCC" Placeholder="Input field" TextColor="Black"/>
        </Frame>

        <Button Text="Cancel" BackgroundColor="#1E50A2" TextColor="White" Clicked="OnCancelClicked"/>
        <Button Text="Login" BackgroundColor="#1E50A2" TextColor="White" Clicked="OnSaveClicked"/>
        

    </StackLayout>
</ContentPage>
